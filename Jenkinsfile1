pipeline {
    agent {
        docker {
            image 'maven:3.8.1-adoptopenjdk-11'
            label 'my-defined-label'
            args '-v /tmp:/tmp -u root:root'
        }
    }
    stages {
        stage('setup') {
            steps {
                sh 'echo "Setting up environment"'
                // Add any other setup steps you may need in this stage
            }
        }
        stage('test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
